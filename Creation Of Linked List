import java.util.*;
public class main{
    public static class node{
        int data;
        node.next;
    }
    public static class linkedlist{
        Node head;
        Node tail;
        Node Size;
    }
}
/*why linked list because array requires contiguous memory allocation
explain with help of stack and heap diagram
base address
node ---->data/next node address
Disadvantages of linked list it requires 2 times more memory than array*/